## Instructions for using components when creating designs

1. We want to use components from highlevel package whenever possible it would always be our first preference
   2.If we don't have components avaialbel in highrise package then we need to ensure to use ghl-ui package components
2. If something is not possible with ghl-ui aswell only then we can write custom css it is our last resort

We need to ensure to not use much of custom css i

##  Using proper comments for hardcoded dummy data

1. We will be building screens for prototying here so whenever we are using hardcoded dummy data please ensure to add proper comments so that when we actually build the screens the AI Coding agent can pick up on the same
   :
# Text instructions
1. Use only sentense case
# 