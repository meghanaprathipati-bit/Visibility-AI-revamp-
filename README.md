# Visibility AI New

An AI-powered multi-engine visibility platform that helps businesses improve SEO, AI search visibility, local listings, and online discoverability through a conversational interface that not only identifies issues but also executes fixes automatically.

---

## Getting started

```bash
npm install
npm run dev
```

All your work goes in `src/pages/`. Add new screens there and wire them up in `src/App.jsx`.
